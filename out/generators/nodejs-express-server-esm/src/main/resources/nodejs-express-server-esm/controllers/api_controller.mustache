/**
 * The {{{classname}}}Controller file is a very simple one, which does not need to be changed manually,
 * unless there's a case where business logic reoutes the request to an entity which is not
 * the service.
 * The heavy lifting of the Controller item is done in Request.js - that is where request
 * parameters are extracted and sent to the service, and where response is handled.
 */

// Updated today
import Controller from './Controller.js'
import * as services from '../services/index.js'

{{#operations}}
{{#operation}}
/**
    * {{{operationId}}}
    * {{{summary}}}
    * {{{description}}}
    * @param {Request} request - Request object
    * @param {Response} response - Response object
    */
export const {{operationId}} = async (request, response) => {
  await Controller.handleRequest(request, response, services.{{operationId}})
}

{{/operation}}
{{/operations}}
